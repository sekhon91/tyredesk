import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "uk.co.buildease.app",
  appName: "buildease",
  webDir: ".output/public",
};

export default config;
